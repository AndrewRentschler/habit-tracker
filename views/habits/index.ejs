<%- include('../partials/html-head') %>
<%- include('../partials/nav') %>

<section>
  <h1>Habits</h1>
  <ul>
    <% habit.forEach(habit => { %>
      <li>
        <a href=""><%= habit.btnName %> - <%= habit.currentStreak %></a>
        <%= habit.btnName %> - <%= habit.currentStreak %>
        <a href="habits/<%= habit._id %>/edit">
        <button>edit</button>
        </a>
      </li>
    <% }) %>
  </ul>
  <% if (user) { %>
    <h2>Add a Habit to Track</h2>
    <form action="/habits" method="POST">
      <label>
        Name:
        <input type="text" name="btnName" autocomplete="off">
      </label>
      
      <button type="submit">Add</button>
    </form>
  <% } %>
</section>
<%- include('../partials/footer') %>